export interface Playlist {
    name: string;
    image: string;
    owner: string;
    id: string;
}

export interface Songs {
    artist: string;
    songName: string;
    preview: string;
    image: string;
    url: string;
}